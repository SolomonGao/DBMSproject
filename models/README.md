# Models

This branch intentionally includes only the final lightweight THP checkpoint:

- `thp_gdelt.pt`: neural Transformer Hawkes Process checkpoint used by the Forecast page and `/api/v1/data/forecast`.

Ignored local artifacts include training datasets, sweep checkpoints, logs, backup checkpoints, and ChromaDB files.
